Drop the product screenshots here. The "See it in action" gallery on /features
loads each one by filename:

  login.png          -> Login screen
  dashboard.png      -> Dashboard screen
  profile.png        -> Employee Profile screen
  reimbursement.png  -> Reimbursement screen
  attendance.png     -> Attendance screen
  leave.png          -> Leave screen

(ai-matching is a link tab to https://app.procor.io/ai-matching, so no image needed.)

Until an image is present, the gallery shows a placeholder with the expected path.
Use PNG (16:9 works best; the frame is aspect-video and uses object-cover).
